import pandas as pd\nimport matplotlib.pyplot as plt\n\nclass PlayerAnalysis:\n    def __init__(self, player_data):\n        self.player_data = player_data\n\n    def get_top_players(self, stat, top_n=5):\n        return self.player_data.nlargest(top_n, stat)\n\n    def plot_stat_distribution(self, stat):\n        plt.figure(figsize=(10, 6))\n        plt.hist(self.player_data[stat], bins=20, alpha=0.7, color='blue')\n        plt.title(f'Distribution of {stat}')\n        plt.xlabel(stat)\n        plt.ylabel('Frequency')\n        plt.grid(True)\n        plt.show()\n\n    def compare_players(self, player_names):\n        comparison_data = self.player_data[self.player_data['Player'].isin(player_names)]\n        return comparison_data.set_index('Player')\n\n# Example usage (uncomment to use):\n# player_data = pd.read_csv('path_to_player_statistics.csv')  # Load player data\n# analysis = PlayerAnalysis(player_data)\n# top_scorers = analysis.get_top_players('Goals')  # Get top 5 players by goals\n# analysis.plot_stat_distribution('Assists')  # Plot distribution of assists\n# comparison = analysis.compare_players(['Player A', 'Player B'])  # Compare two players\n